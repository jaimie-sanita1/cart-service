$kind: http-request
url: "{{baseUrl}}/carts/{{cartId}}"
method: DELETE
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: pm.test("Status is 204", () => pm.response.to.have.status(204));
    language: text/javascript
examples: ./.resources/7- Delete a cart.resources/examples
order: 7000
