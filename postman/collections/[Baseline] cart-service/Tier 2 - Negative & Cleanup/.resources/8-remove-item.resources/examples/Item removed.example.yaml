$kind: http-example
request:
  url: "{{baseUrl}}/carts/:cartId/items/:itemId"
  method: DELETE
  pathVariables:
    cartId: "{{secondCartId}}"
    itemId: "{{removableItemId}}"
  body:
    type: text
    content: ""
response:
  statusCode: 204
  statusText: No Content
  body:
    type: text
    content: ""
order: 1000
