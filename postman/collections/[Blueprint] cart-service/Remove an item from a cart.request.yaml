$kind: http-request
name: Remove an item from a cart
url: "{{baseUrl}}/carts/:cartId/items/:itemId"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  - key: cartId
    value: "{{cartId}}"
    disabled: false
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "default": "<string>"
          }
        },
        "required": [
          "value"
        ]
      }
  - key: itemId
    value: "{{itemId}}"
    disabled: false
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "default": "<string>"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status is 204', () => pm.response.to.have.status(204));
      pm.test('Response body is empty', () => {
        pm.expect(pm.response.text() || '').to.eql('');
      });

      pm.collectionVariables.unset("itemId");
      pm.collectionVariables.unset("cartId");
    language: text/javascript
  - type: beforeRequest
    code: ""
    language: text/javascript
examples: ./.resources/Remove an item from a cart.resources/examples
order: 5000
